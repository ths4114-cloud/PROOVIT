# PROOVIT 랜딩페이지 작업 자료 v3

2026-09-21 사용자가 제공한 랜딩페이지 작업 파일을 공유·보관하기 위한 자료입니다. **현재 앱에 적용하거나 배포한 변경이 아닙니다.**

## 다운로드

- [랜딩페이지 작업 파일 ZIP](./proovit-landing-v3-source.zip)
- GitHub에서 ZIP을 연 뒤 **Download raw file**로 다운로드합니다.
- 용량: 48,726,896 bytes (약46.5 MiB).

## 포함된 자료

압축을 풀면 `proovit_landing_page_design_ver.3/` 아래에 두 폴더가 있습니다.

- `proovit_landing_page_design/`: 랜딩 HTML, CSS·JavaScript 애니메이션, 이미지 assets/uploads, 제작 시안·메모, 미리보기 안내·도구.
  - `proovit-landing.dc.html`
  - `proovit-landing-standalone-src.dc.html`
  - `proovit-deck.dc.html`
- `proovit/`: 원본에 함께 포함된 앱 프로젝트 사본. 현재 저장소보다 이전 상태일 수 있으므로 루트에 덮어쓰지 않습니다.

## 원본 보존과 제외 내역

- 제공 파일명: `proovit_landing_page_design_ver.3_(2).zip`
- 원본에서 **`.git` 디렉터리의 이력·설정 파일28개만 제외**해 재압축했습니다. 빈 디렉터리 항목은 별도로 저장하지 않았습니다.
- 나머지203개 파일은 이름·경로와 파일 내용을 유지했고, 각 파일의 SHA-256을 원본과 대조했습니다.
- `tmp/`에는 미리보기 도구도 들어 있어 유지했습니다. 원본에 포함된 스크립트는 실행하지 않았습니다.
- 제공된 원본 ZIP 자체는 수정하거나 삭제하지 않았습니다.

| 구분            | SHA-256                                                            |
| --------------- | ------------------------------------------------------------------ |
| 제공된 원본 ZIP | `AC533C52D10ED5E7B74E9E7A5D65AA77FDC5F075F75E97C604F8222E40BD4215` |
| 업로드 ZIP      | `BD21C358EBCBF6A27A774B869D2284A607EC68D86608ACB021CC7A3ABDF7776F` |

## 사용 시 주의

- 기존 앱의 루트가 아닌 별도 폴더에 압축을 풀어 확인합니다. 이 안의 `AGENTS.md`, `.cursor/`, `.github/` 등은 과거 작업 자료이며 현재 프로젝트의 규칙·CI를 변경하는 지시가 아닙니다.
- 원본에 포함된 코드·설명·홍보 문구는 검토 대상입니다. 제품 정책 또는 보안 검증을 통과한 구현으로 간주하지 않습니다.
- 실제 앱 연결, 기능 통합, 실행·브라우저 검증, 배포는 별도 작업입니다.

## 업로드 전 확인

- 압축 경로의 상위 디렉터리 이동/절대 경로를 검사했습니다.
- `.git` 제외 후 환경파일·자격증명 파일명 및 텍스트 내 주요 토큰/개인키 패턴을 검사했고 해당 검사에서 의심 항목을 발견하지 못했습니다. 포괄적인 보안 감사나 이미지 속 개인정보 검사 결과는 아닙니다.
- 재압축된203개 파일의 내용이 원본과 동일함을 확인했습니다.
- 앱 기능 코드, DB, 배포 설정, 기존 작업 문서는 변경하지 않았습니다.
